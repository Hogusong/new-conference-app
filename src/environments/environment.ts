// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCThv79snE5-5SVGEx4DtIJ2_KR6MTTTjQ",
    authDomain: "conference-app-ac369.firebaseapp.com",
    databaseURL: "https://conference-app-ac369.firebaseio.com",
    projectId: "conference-app-ac369",
    storageBucket: "conference-app-ac369.appspot.com",
    messagingSenderId: "702229496983"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
